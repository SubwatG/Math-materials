# KaTeX 0.16.9 (vendored)

Offline math for `*-purehtml.html` decks. Paths are relative to `slides/`:

- `katex.min.css` (+ `fonts/`)
- `katex.min.js`
- `auto-render.min.js`

Refresh from npm: `npm pack katex@0.16.9` and copy `package/dist/*` as documented in `slides/registry.yaml`.